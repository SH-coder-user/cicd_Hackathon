server:
  port: 8080

wsie:
  callback:
    api-key: "비워둠"
  py:
    base-url: "http://py:8000"

spring:
  servlet:
    multipart:
      max-file-size: 10MB
      max-request-size: 10MB
springdoc:
  swagger-ui:
    path: /swagger-ui
  api-docs:
    path: /v3/api-docs
